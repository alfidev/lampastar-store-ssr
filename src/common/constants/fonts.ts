import { Roboto } from 'next/font/google';

export const fontRobotoRegular = Roboto({ subsets: ['cyrillic'], weight: '400' });

export const fontRobotoBold = Roboto({ subsets: ['cyrillic'], weight: '500' });

export const fontRobotoBlack = Roboto({ subsets: ['cyrillic'], weight: '700' });
